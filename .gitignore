# Build artifacts
/target

# Python bytecode and interpreter caches
__pycache__/
*.py[cod]

# PQC key material — NEVER commit
*.keys/
*.keys
dk.bin
sk.bin
vk.bin
*.pem
*.key
*.priv

# Encrypted databases and signatures
*.db
*.db.enc
*.sig
!release/provenance/manifest.sha256.sig

# Environment and secrets
.env
.env.*
credentials*
*.secret

# Communication and contact files — private, not for repo
CONTACTS.md
NOTIFICATIONS.md
OUTREACH.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.iml

# Node/promptfoo local tooling
node_modules/
.promptfoo/
redteam.yaml
promptfoo-results/
redteam-results/


# Crawl output
crawl_data/

# Test output binaries
*.bin
*.out
a.out

# Compiled / bootstrap / debug ELF binaries — keep sources only
*.elf
apps.zip
jstar/jstar[0-9]
jstar/jstar_c
jstar/jstar_canonical
jstar_bootstrap_out/jstar[123]

# Deep agent config — not part of this project
agent.py
.claude/
*.json
!package.json
!package-lock.json
!security/**/*.json
!release/provenance/*.json
!release/provenance/release-manifest.pub.pem
!release/security/*.json
llm_checkpoints/
logs/
rr_db.json
.qwen/
.cache_jstar_target/

# Regression harness artifacts
tests/regression/**/*.elf
tests/regression/**/work.*
tests/regression/**/actual.*.log
